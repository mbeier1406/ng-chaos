import { HighlightIfMany } from './highlight-if-many';

describe('HighlightIfMany', () => {
  it('should create an instance', () => {
    const directive = new HighlightIfMany();
    expect(directive).toBeTruthy();
  });
});
